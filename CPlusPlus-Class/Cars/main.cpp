#include <iostream>
#include "Car.h"
#include <time.h>
#include <set>
#include "lib.h"
#include <vector>
#include "Vehicule.h"
#include "Flying_Vehicule.h"
#include "Vehicule_Wheels.h"
#include "Plane.h"
#include "Helicopter.h"
#include "Motorbike.h"
#include "Bike.h"




int main()
{
    /*
    srand(time(NULL));
    std::set<int> ints;
    std::set<Car, std::less<>> car_catalog = { {"Pharaoh", 1986 , random_int2()}, {"High Heels", 2200, random_int2()}, {"Fogger", 1850, random_int2()} };
  
    for (Car car : car_catalog)
    {
        if (car.get_random() % 3 == 0)
        {
            ints.insert(car.get_random());
        }
    }

    while (ints.empty() == false)
    {
        std::cout << "Here are/is the heretic(s)!" << std::endl;
        for (int integer : ints)
        {
            std::cout << integer << " ";
        }

        std::cout << std::endl << std::endl;

        ints.erase(ints.begin(), ints.end());
    }

    std::cout << "we killed them all";
    */


    /*
    Flying_Vehicule fv;
    fv.see_infos();
    Plane plane;
    plane.see_infos();
    Helicopter helios;
    helios.see_infos();

    Vehicule_Wheels wv;
    wv.see_infos();
    Motorbike motorb;
    motorb.see_infos();
    Bike bike;
    bike.see_infos();
    */

    
    
  
    
}
